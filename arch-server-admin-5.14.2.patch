diff -uprN AdminLTE-5.14.2/scripts/pi-hole/php/FTL.php AdminLTE-5.14.2.cust/scripts/pi-hole/php/FTL.php
--- AdminLTE-5.14.2/scripts/pi-hole/php/FTL.php	2022-09-04 15:36:25.000000000 +0200
+++ AdminLTE-5.14.2.cust/scripts/pi-hole/php/FTL.php	2022-09-05 00:10:46.248025295 +0200
@@ -10,7 +10,7 @@
 const DEFAULT_FTLCONFFILE = '/etc/pihole/pihole-FTL.conf';
 const DEFAULT_FTL_IP = '127.0.0.1';
 const DEFAULT_FTL_PORT = 4711;
-const DEFAULT_FTL_PORTFILE = '/run/pihole-FTL.port';
+const DEFAULT_FTL_PORTFILE = '/run/pihole-ftl/pihole-FTL.port';
 
 function piholeFTLConfig($piholeFTLConfFile = DEFAULT_FTLCONFFILE, $force = false)
 {
diff -uprN AdminLTE-5.14.2/scripts/pi-hole/php/func.php AdminLTE-5.14.2.cust/scripts/pi-hole/php/func.php
--- AdminLTE-5.14.2/scripts/pi-hole/php/func.php	2022-09-04 15:36:25.000000000 +0200
+++ AdminLTE-5.14.2.cust/scripts/pi-hole/php/func.php	2022-09-05 00:10:46.278025458 +0200
@@ -148,7 +148,7 @@ function pihole_execute($argument_string
     $escaped = escapeshellcmd($argument_string);
     $output = null;
     $return_status = -1;
-    $command = 'sudo pihole '.$escaped;
+    $command = 'sudo /usr/bin/pihole '.$escaped;
     exec($command, $output, $return_status);
     if ($return_status !== 0) {
         trigger_error("Executing {$command} failed.", E_USER_WARNING);
diff -uprN AdminLTE-5.14.2/scripts/pi-hole/php/gravity.sh.php AdminLTE-5.14.2.cust/scripts/pi-hole/php/gravity.sh.php
--- AdminLTE-5.14.2/scripts/pi-hole/php/gravity.sh.php	2022-09-04 15:36:25.000000000 +0200
+++ AdminLTE-5.14.2.cust/scripts/pi-hole/php/gravity.sh.php	2022-09-05 00:10:46.284025491 +0200
@@ -34,7 +34,7 @@ function echoEvent($datatext)
     echo 'data: '.implode("\ndata: ", explode("\n", $datatext))."\n\n";
 }
 
-$proc = popen('sudo pihole -g', 'r');
+$proc = popen('sudo /usr/bin/pihole -g', 'r');
 while (!feof($proc)) {
     echoEvent(fread($proc, 4096));
 }
diff -uprN AdminLTE-5.14.2/scripts/pi-hole/php/queryads.php AdminLTE-5.14.2.cust/scripts/pi-hole/php/queryads.php
--- AdminLTE-5.14.2/scripts/pi-hole/php/queryads.php	2022-09-04 15:36:25.000000000 +0200
+++ AdminLTE-5.14.2.cust/scripts/pi-hole/php/queryads.php	2022-09-05 00:10:46.280025469 +0200
@@ -50,7 +50,7 @@ if (isset($_GET['exact'])) {
     $exact = '';
 }
 
-$proc = popen('sudo pihole -q -adlist '.$url.' '.$exact, 'r');
+$proc = popen('sudo /usr/bin/pihole -q -adlist '.$url.' '.$exact, 'r');
 while (!feof($proc)) {
     echoEvent(fread($proc, 4096));
 }
diff -uprN AdminLTE-5.14.2/scripts/pi-hole/php/savesettings.php AdminLTE-5.14.2.cust/scripts/pi-hole/php/savesettings.php
--- AdminLTE-5.14.2/scripts/pi-hole/php/savesettings.php	2022-09-04 15:36:25.000000000 +0200
+++ AdminLTE-5.14.2.cust/scripts/pi-hole/php/savesettings.php	2022-09-05 00:10:46.282025480 +0200
@@ -424,7 +424,7 @@ if (isset($_POST['field'])) {
             if (isset($_POST['webtheme'])) {
                 global $available_themes;
                 if (array_key_exists($_POST['webtheme'], $available_themes)) {
-                    exec('sudo pihole -a theme '.$_POST['webtheme']);
+                    exec('sudo /usr/bin/pihole -a theme '.$_POST['webtheme']);
                 }
             }
             $success .= 'The webUI settings have been updated';
diff -uprN AdminLTE-5.14.2/scripts/pi-hole/php/sidebar.php AdminLTE-5.14.2.cust/scripts/pi-hole/php/sidebar.php
--- AdminLTE-5.14.2/scripts/pi-hole/php/sidebar.php	2022-09-04 15:36:25.000000000 +0200
+++ AdminLTE-5.14.2.cust/scripts/pi-hole/php/sidebar.php	2022-09-05 00:10:46.246025284 +0200
@@ -247,12 +247,6 @@
                                 Tail FTL.log
                             </a>
                         </li>
-                        <!-- Generate debug log -->
-                        <li class="<?php if ($scriptname === 'debug.php') { ?> active<?php } ?>">
-                            <a href="debug.php">
-                                <i class="fa fa-fw menu-icon fa-ambulance"></i> Generate debug log
-                            </a>
-                        </li>
                         <!-- Network -->
                         <li class="<?php if ($scriptname === 'network.php') { ?> active<?php } ?>">
                             <a href="network.php">
diff -uprN AdminLTE-5.14.2/scripts/pi-hole/php/tailLog.php AdminLTE-5.14.2.cust/scripts/pi-hole/php/tailLog.php
--- AdminLTE-5.14.2/scripts/pi-hole/php/tailLog.php	2022-09-04 15:36:25.000000000 +0200
+++ AdminLTE-5.14.2.cust/scripts/pi-hole/php/tailLog.php	2022-09-05 00:10:46.252025317 +0200
@@ -31,9 +31,9 @@ function formatLine($line)
 // usage of f-streams will be much faster for
 // files as large as the pihole.log
 if (isset($_GET['FTL'])) {
-    $file = fopen('/var/log/pihole/FTL.log', 'r');
+    $file = fopen('/run/log/pihole-ftl/pihole-FTL.log', 'r');
 } else {
-    $file = fopen('/var/log/pihole/pihole.log', 'r');
+    $file = fopen('/run/log/pihole/pihole.log', 'r');
 }
 
 if (!$file) {
diff -uprN AdminLTE-5.14.2/scripts/pi-hole/php/update_checker.php AdminLTE-5.14.2.cust/scripts/pi-hole/php/update_checker.php
--- AdminLTE-5.14.2/scripts/pi-hole/php/update_checker.php	2022-09-04 15:36:25.000000000 +0200
+++ AdminLTE-5.14.2.cust/scripts/pi-hole/php/update_checker.php	2022-09-05 00:10:46.276025447 +0200
@@ -15,17 +15,17 @@ $versionsfile = '/etc/pihole/versions';
 
 if (!is_readable($versionsfile)) {
     $core_branch = 'master';
-    $core_current = 'N/A';
+    $core_current = '';
     $core_update = false;
 
     $web_branch = 'master';
-    $web_current = 'N/A';
+    $web_current = '';
     $web_update = false;
 
-    $FTL_current = 'N/A';
+    $FTL_current = '';
     $FTL_update = false;
 
-    $docker_current = 'N/A';
+    $docker_current = '';
     $docker_update = false;
 } else {
     $versions = parse_ini_file($versionsfile);
@@ -34,32 +34,32 @@ if (!is_readable($versionsfile)) {
     // Check if on a dev branch
     $core_branch = $versions['CORE_BRANCH'];
     if ($core_branch !== 'master') {
-        $core_current = 'vDev';
+        $core_current = '';
         $core_commit = $versions['CORE_VERSION'];
     } else {
-        $core_current = explode('-', $versions['CORE_VERSION'])[0];
+        $core_current = '';
     }
 
     // Get Pi-hole web branch / version / commit
     $web_branch = $versions['WEB_BRANCH'];
     if ($web_branch !== 'master') {
-        $web_current = 'vDev';
+        $web_current = '';
         $web_commit = $versions['WEB_VERSION'];
     } else {
-        $web_current = explode('-', $versions['WEB_VERSION'])[0];
+        $web_current = '';
     }
 
     // Get Pi-hole FTL (not a git repository)
     $FTL_branch = $versions['FTL_BRANCH'];
     if (substr($versions['FTL_VERSION'], 0, 4) === 'vDev') {
-        $FTL_current = 'vDev';
+        $FTL_current = '';
         $FTL_commit = $versions['FTL_VERSION'];
     } else {
-        $FTL_current = $versions['FTL_VERSION'];
+        $FTL_current = '';
     }
 
     // Get Pi-hole Docker Tag, if available
-    $docker_current = htmlspecialchars(getenv('PIHOLE_DOCKER_TAG'));
+    $docker_current = '';
 
     // Get data from GitHub
     $core_latest = $versions['GITHUB_CORE_VERSION'];
@@ -78,7 +78,7 @@ if (!is_readable($versionsfile)) {
     // Version comparison
     if ($docker_current) {
         // It's a container: do not check for individual component updates
-        if ($docker_current == 'nightly' || $docker_current == 'dev') {
+        if ($docker_current = '';
             // Special container - no update messages
             $docker_update = false;
         } else {
@@ -96,9 +96,9 @@ if (!is_readable($versionsfile)) {
 }
 
 // URLs for the links
-$coreUrl = 'https://github.com/pi-hole/pi-hole/releases';
-$webUrl = 'https://github.com/pi-hole/AdminLTE/releases';
-$ftlUrl = 'https://github.com/pi-hole/FTL/releases';
+$coreUrl = 'https://aur.archlinux.org/packages/pi-hole-server';
+$webUrl = 'https://aur.archlinux.org/packages/pi-hole-server';
+$ftlUrl = 'https://aur.archlinux.org/packages/pi-hole-ftl';
 $dockerUrl = 'https://github.com/pi-hole/docker-pi-hole/releases';
 
 // Version strings
